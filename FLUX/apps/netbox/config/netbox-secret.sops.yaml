apiVersion: v1
kind: Secret
metadata:
  name: netbox-secret
  namespace: netbox
stringData:
  values.yaml: |
    superuser:
      name: admin
      email: ostbym@proton.ne
      password: SECURE_PASSWORD
    postgresql:
      external:
        password: "CHANGE_ME"
    redis:
      tasks:
        password: "CHANGE_ME"
      caching:
        password: "CHANGE_ME"
